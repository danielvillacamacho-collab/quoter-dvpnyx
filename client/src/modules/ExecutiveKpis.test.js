/**
 * Tests for the Executive KPI strip. The component still exists in App.js
 * but is not currently rendered on any route — the old dashboard was removed
 * and will be rebuilt later. These tests are skipped until the new dashboard
 * integrates the strip.
 */
import React from 'react';

describe.skip('ExecutiveKpis strip (pending dashboard rebuild)', () => {
  it('placeholder', () => {});
});
