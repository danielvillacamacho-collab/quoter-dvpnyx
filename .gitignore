node_modules/
.env
.env.local
.env.*.local
client/build/
*.log
.DS_Store
# Runtime / server-local state (never commit)
letsencrypt/
deploy-history.log
.claude/
# Lambda build artifacts (generados por CI, no versionar)
serverless/dist/
serverless/packages/infra/src/*.js
serverless/packages/infra/src/*.js.map
serverless/packages/infra/src/*.zip

# Test coverage artifacts
coverage/
.nyc_output/
# Editor / OS cruft
.vscode/
.idea/
*.swp
Thumbs.db
# Local-only directories dropped by agents / designers
UI de claude design/
# User-generated docs (manuales, briefs) — el "source of truth" vive en docs/*.md.
# Los .docx son outputs/inputs efímeros del PO/UX, no source code.
docs/*.docx
*.docx
